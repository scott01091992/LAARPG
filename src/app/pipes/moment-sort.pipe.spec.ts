import { MomentSortPipe } from './moment-sort.pipe';

describe('MomentSortPipe', () => {
  it('create an instance', () => {
    const pipe = new MomentSortPipe();
    expect(pipe).toBeTruthy();
  });
});
