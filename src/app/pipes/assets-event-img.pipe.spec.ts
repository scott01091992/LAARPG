import { AssetsEventImgPipe } from './assets-event-img.pipe';

describe('AssetsEventImgPipe', () => {
  it('create an instance', () => {
    const pipe = new AssetsEventImgPipe();
    expect(pipe).toBeTruthy();
  });
});
