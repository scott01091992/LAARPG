import { AssetsIconSvgPipe } from './assets-icon-svg.pipe';

describe('AssetsIconSvgPipe', () => {
  it('create an instance', () => {
    const pipe = new AssetsIconSvgPipe();
    expect(pipe).toBeTruthy();
  });
});
